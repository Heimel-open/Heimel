# Alexander long-horizon human–AI convergence — derived evidence only

Date: 2026-08-23  
Status: `DERIVED_CONVERGENCE_NOT_RUN_DATA`

## Purpose

Preserve the independent Alexander/AION material as a research convergence anchor without allowing it to contaminate MELLOMROM RUN-001 as raw empirical input.

## Source boundary

Sources inspected from the user's file library:

- `ANALYSE_ALEX_DENKMETHODEN_GESAMTARCHIV_V1.md`
- `HERKUNFTSANALYSE_DENKMUSTER_MENSCH_KI_V1_0.md`

These are analyses/reconstructions, not the raw 164-chat episode corpus.

Therefore:

```text
Alexander analysis
!=
raw MELLOMROM episode
!=
RUN-001 empirical input
```

The empirical builder now hard-requires `provenance=raw`.

## Derived convergence observations

The Gesamtarchiv analysis maps chat 035 across multiple distinct functions:

- observation / phase-separated evaluation;
- dialogic calibration and AI as resonance surface;
- self-test / field experiment / feeling validation;
- reality, contradiction and function testing.

It maps chats 128 and 131 to both:

- reality / contradiction / function testing;
- coupling and total-flow inspection.

That overlap is useful because the candidate RUN-001 target is not merely transcript sophistication. It is whether historically formed correction and coupling patterns carry later task-relevant function.

The same analysis describes a repeated cycle in the early `Mittdenker` work:

```text
idea
-> prototype
-> self-test
-> experienced effect
-> causal analysis
-> variant space
-> rebuild
-> retest
```

It also records that a technically plausible interaction was rebuilt after the user experienced the effect as wrong. This is a concrete example of interaction history changing later architecture rather than merely being stored as passive text.

The Herkunftsanalyse separately classifies several structures as interaction-emergent and describes a closed feedback loop in which human thinking becomes visible through the LLM, is formalized, fed back into later interaction, and changes subsequent working behavior.

## Why this matters to the observatory

This material supports the choice of human–AI long-horizon lineage as a natural falsification target.

It does not establish the target effect.

The empirical question remains:

> Does intact historical lineage improve blinded downstream reconstruction, transfer or correction behavior relative to information-volume-matched shuffled and endpoint-matched controls?

Only raw episodes can answer that RUN-001 question.

## Integrity rule

Derived/reconstructed material may:

- motivate task design;
- nominate source chats;
- define candidate phenomena;
- provide external convergence.

It may not:

- populate the raw episode pack;
- be relabeled as original dialogue;
- satisfy the empirical provenance gate;
- be scored as if it were a preserved historical trajectory.

Compact rule:

> **Use reconstruction to decide where to look. Use primary interaction history to decide what happened.**
