# VAIG Core Test Run — 2026-07-31

## Environment

- Python: 3.12.3
- Platform: Linux 7.0.0-28-generic
- Base branch: `main`
- Commit: `5c350e29f6d079308c6b35d158bc57190c079869`
- Package: non-editable `vaig-0.3.0` wheel
- Heavy optional dependencies absent: NumPy, Torch, sentence-transformers

## Commands

```bash
python3 -m pip --python /tmp/vaig-core-venv/bin/python \
  install --no-deps --force-reinstall .

cd /tmp
/tmp/vaig-core-venv/bin/python -m pytest /home/njaal/VAIG \
  -q --tb=short -W error::DeprecationWarning
```

## Result

```text
615 passed in 2.54s
```

Additional package check:

```text
vaig.__version__ = 0.3.0
importlib.metadata.version("vaig") = 0.3.0
```

## Scope of evidence

This run proves that the repository's 615 collected tests pass on Python 3.12
against an installed wheel, with deprecated Python APIs treated as failures. It
also proves that the core test installation does not require the optional ML
stack.

It does not prove production hardening, external execution enforcement,
hardware-backed or production WORM storage, regulatory conformity, distributed
operation, on-chain receipts, zero-knowledge proofs, or complete formal
verification.
