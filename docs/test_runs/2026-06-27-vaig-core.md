# VAIG Core Test Run — 2026-06-27

## Command

```bash
pytest -q tests/
```

## Environment

- Python: 3.11.15
- Platform: Linux 6.18.5
- Branch: `claude/claude-md-docs-74t2jg`
- Commit: `75359b1c008adf75232d862c7327d893aa269732`
- Date: 2026-06-27T16:25 UTC

## Result

```
295 passed, 0 failed, 0 errors, 0 skipped
```

## Modules covered

| Module | Status |
|---|---|
| `vaig.ensemble` (VAIGEnsemble, D0–D4 distrust levels) | tested |
| `vaig.orchestrator` (VAIGOrchestrator) | tested |
| `vaig.worm` (SHA-256 hash-chained WORM log) | tested |
| `vaig.recovery` (RecoveryManager) | tested |
| `vaig.cakm` (CAKM, AlertLevel) | tested |
| `vaig.council` (CouncilQueue) | tested |
| `vaig.skjaersilden` (Skjaersilden) | tested |
| `vaig.delta_sandbox` (DeltaBoxSandbox) | tested |
| `vaig.agent_loop` (AgentLoop, GateEvent, GateDecision) | tested |
| `vaig.rrp` (RefusalLifecycle, EvidenceCondition, RiskContract, Receipt) | tested |
| `vaig.execution_boundary` | tested |
| `vaig.value_meter` (ValueMeter, AV, ControlYield) | tested |
| `vaig.economy` (EconomyGate, TokenMeter, ContextBudget, router) | tested |
| `src/authority_gate.py` (AuthorityGate, delegation, expiry) | tested |
| `src/why_gate.py` (WHY Gate v2, 4-dimension score model) | tested |

## Failing tests

None.

## External-claim limit

This test run proves that the listed modules execute correctly under unit test conditions with in-process fixtures.

This does not prove:
- enterprise production hardening
- regulatory compliance or conformity assessment
- ACS/VACS standardization (draft proposal)
- hardware-locked WORM storage
- AES-256 encryption (current WORM uses XOR key stream — not production crypto)
- distributed or replicated audit trail
- full formal verification (TLA+ specs live in valo-v5-core, not this repo)

Allowed claim from this run: VAIG has a testable, green core across 295 tests spanning pre-intent evidence gating, refusal lifecycle, WORM audit, agent-loop governance, cost-value economics, and authority delegation.
