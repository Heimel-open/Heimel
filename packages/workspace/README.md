# VALO governed workspace reference

This package contains the authority-neutral `GovernedWorkspaceContract`,
delivery conformance checks, and fail-closed state-admission binding extracted
from the private VALO platform boundary.

It does not create authority, execute effects, persist runtime state, expose
credentials, or include workspace adapters and transaction orchestration.
