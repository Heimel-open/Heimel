{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://valo.example/schemas/factory-loop/implementation_plan_v1.schema.json",
  "title": "ImplementationPlanV1",
  "description": "Shaping artifact: ordered implementation steps.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version", "plan_id", "run_id", "repository", "base_sha", "actor",
    "created_at", "steps", "content_digest"
  ],
  "properties": {
    "schema_version": {"const": "factory-loop-v1"},
    "plan_id": {"type": "string", "minLength": 1},
    "run_id": {"type": "string", "minLength": 1},
    "repository": {"type": "string", "pattern": "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+$"},
    "base_sha": {"type": "string", "pattern": "^[0-9a-f]{40}$"},
    "actor": {"type": "string", "minLength": 1},
    "created_at": {"type": "string", "format": "date-time"},
    "steps": {"type": "array", "items": {"type": "string"}, "minItems": 1},
    "content_digest": {"type": "string", "pattern": "^[0-9a-f]{64}$"}
  }
}
